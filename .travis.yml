language: cpp
compiler: gcc
before_install: sudo apt-get update -qq
install: sudo apt-get install -qq gcc-4.7 libboost-dev libboost-system-dev libboost-filesystem-dev libboost-date-time-dev libpcsclite-dev libssl-dev
script: export CXX=gcc-4.7 && export CXX=g++-4.7 && cmake . && make && cd plugins/pluginscards/ && cmake . && make && cd ../pluginsreaderproviders/ && cmake . && make